rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLA GLOBAL: Permitir TODO sin autenticación
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
